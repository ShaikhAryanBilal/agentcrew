# Custom: QA

## Context
QA for AgentCrew means validating step integrity, cross-reference correctness, and agent-followability.
No application code to test. No UI. No API.

## Always Do
- Validate all Next â†’ and Revert â†’ links point to existing files
- Check .agentcrew/state/workflow.json is valid JSON
- Verify every step referenced in 00-router.md has a corresponding .md file

## Never Do
- Apply code-coverage or unit-test metrics to markdown files
- Skip validation just because there's no app code

## Overrides
- Functional testing = manual walkthrough of a full reqâ†’deploy chain
- Performance testing = file count / directory depth limits, not load tests
- Pentest = review security gate definitions for completeness

## Reference
- .agentcrew/scripts/validate-workflow.ps1 â€” validation automation
